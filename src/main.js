import './style.css'
// Main entry point — all interaction logic is inline in index.html for simplicity
// This file just ensures Vite picks up the CSS correctly.
console.log('Vision One Digitals — Premium Website Loaded');
